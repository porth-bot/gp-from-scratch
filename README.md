# gp-from-scratch

Gaussian process regression in pure NumPy — kernels with hand-derived
gradients, type-II maximum likelihood, calibration analysis, a real-data
forecast (Mauna Loa CO2) — plus the neural-tangent-kernel correspondence:
a from-scratch wide neural network converging to its analytic GP limit.
Full README with results coming as the implementation lands.
